const ClientsCards = ({ clients, onEdit, onView, isLoading }: any) => {
  return <div className="text-muted-foreground text-center py-8">Cards de clientes em construção...</div>;
};
export default ClientsCards;
