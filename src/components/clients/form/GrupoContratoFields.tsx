import { UseFormReturn } from 'react-hook-form';
import { ClientFormData } from './clientFormSchema';

interface GrupoContratoFieldsProps { form: UseFormReturn<ClientFormData>; }

export function GrupoContratoFields({ form }: GrupoContratoFieldsProps) {
  return null; // Placeholder - full implementation pending
}
